#
# Makefile.am for installation/scripts
#
#
#

SUBDIRS = Firstboot-Stage
startdir = /etc/init.d
ystartupbindir=$(ystartupdir)/startup

ystartupbin_SCRIPTS = \
	       YaST2.Firstboot 

start_SCRIPTS = firstboot

EXTRA_DIST =  $(start_SCRIPTS) $(ystartupbin_SCRIPTS)
